# 金融知识图谱项目（financialKG）代码报告

> 报告生成日期：2026-07-24
> 报告范围：`/home/huyuan/openclaw` 全仓库（前端 `front/`、后端 `knowledgegraph/`、离线脚本 `lsydata/` 及根目录扫描脚本）

---

## 一、项目概述

### 1.1 项目定位

本项目是一套**金融知识图谱管理与可视化系统**，围绕 A 股上市公司构建图谱，提供节点/关系的增删改查、元知识（含公式与变量）管理、新闻文本关系抽取、图谱统计分析、金融风险案例展示等功能。

### 1.2 技术栈

| 层 | 技术 | 版本 |
|----|------|------|
| 前端 | Vue 3 + Vue Router 4 + Element Plus + axios + D3.js + mathlive | vue ^3.2.13 |
| 后端 | Django + Django REST Framework + SimpleJWT + py2neo | Django 4.2.16 |
| 图数据库 | Neo4j（通过 py2neo / Bolt 协议访问） | - |
| 关系数据库 | SQLite（仅存用户与元知识） | - |
| 异步任务 | Celery + Redis（已配置但实际未在视图中使用） | celery 5.4.0 |
| LLM 推理 | Ollama 本地服务（qwen3.6:27b / qwen2.5:32b） | - |
| 部署 | uWSGI（配置存在但路径过时） | uWSGI 2.0.28 |

### 1.3 仓库结构概览

```
openclaw/
├── front/                      # Vue 3 前端
│   ├── src/
│   │   ├── main.js             # 入口 + axios/ResizeObserver 全局配置
│   │   ├── router/index.js     # 路由 + 登录守卫
│   │   ├── views/              # 24 个页面组件
│   │   ├── data/kg_report.js   # 静态报告数据（约 117MB）
│   │   └── components/
│   ├── public/                 # cross_doc_dataset_updated.json 等静态资源
│   └── vue.config.js           # devServer 代理 /api -> 10.176.22.62:8001
├── knowledgegraph/             # Django 后端
│   ├── knowledgegraph/
│   │   ├── settings.py         # 配置（密钥/Neo4j/CORS/Celery 全硬编码）
│   │   ├── urls.py             # 23 条路由
│   │   ├── views.py            # 全部业务视图（约 1590 行）
│   │   ├── models.py           # CustomUser / MetaKnowledge / Formula / Variable / FormulaVariable
│   │   ├── relation_extractor.py  # 基于 Ollama 的关系抽取器
│   │   ├── serializers.py      # DRF 序列化器
│   │   ├── permissions.py      # IsNeo4jUser / IsMetaKnowledgeUser
│   │   ├── decorators.py       # neo4j_user_required / metaknowledge_user_required
│   │   ├── middleware.py       # 自定义 CORS 中间件（未被注册）
│   │   └── companynameparser/  # 公司名相似度匹配（依赖 BERT）
│   ├── management/commands/    # create_custom_users / create_metaknowledge
│   ├── db.sqlite3
│   └── uwsgi.ini               # 旧部署路径
├── lsydata/data/financedata/   # 离线关系预测脚本（laguanxi.py）
├── scan_*.py                   # 9 个图谱扫描脚本
└── *.md                        # 各类汇报文档
```

---

## 二、后端架构分析

### 2.1 配置层（settings.py）

后端配置位于 [knowledgegraph/knowledgegraph/settings.py](knowledgegraph/knowledgegraph/settings.py)，存在以下关键配置：

- **数据库**：SQLite，文件 `db.sqlite3`
- **认证**：JWT（SimpleJWT）+ Session 双认证
- **默认权限**：`AllowAny`（全局放开）
- **CORS**：白名单含 8 个来源（localhost / 10.176.22.62 / 172.29.192.1）
- **Celery**：`redis://localhost:8000/0`（端口 8000，非标准 Redis 端口）

### 2.2 数据模型（models.py）

定义在 [knowledgegraph/knowledgegraph/models.py](knowledgegraph/knowledgegraph/models.py)，共 5 个模型：

| 模型 | 作用 | 关键字段 |
|------|------|---------|
| `CustomUser` | 自定义用户 | `user_type`（neo4j / metaknowledge） |
| `MetaKnowledge` | 元知识 | `description`, `created_at` |
| `Formula` | 公式 | `meta_knowledge`(FK), `formula_string` |
| `Variable` | 变量 | `variable_name`, `variable_meaning`, `reference_count` |
| `FormulaVariable` | 公式-变量关联 | `formula`(FK), `variable`(FK)，唯一约束 |

**设计亮点**：`FormulaVariable` 重写了 `save()`/`delete()`，自动维护 `Variable.reference_count`，当引用归零时自动删除变量，实现了变量的生命周期管理。

**注意**：图谱中的 `Company`、`Violation`、`Litigation`、`MetaKnowledge` 等节点存放在 **Neo4j** 中，而非 Django ORM，因此 Django 模型与图谱节点是两套独立体系。

### 2.3 URL 路由（urls.py）

路由定义在 [knowledgegraph/knowledgegraph/urls.py](knowledgegraph/knowledgegraph/urls.py)，共 23 条路由，可分为五组：

| 分组 | 路由 | 说明 |
|------|------|------|
| 认证 | `/login/` | JWT 登录 |
| 节点操作 | `/addnode/`, `/deletenode/`, `/querynode/`, `/addnodeexcel/`, `/querynodeexcel/`, `/qynodedtil/`, `/fuzzymatch/`, `/fmatexcel/` | 公司节点 CRUD |
| 关系操作 | `/addrelationshipexcel/`, `/queryrelationship/`, `/getprogress/` | 关系增查 |
| 元知识 | `/meta/allmetaknowledge/`（ViewSet CRUD） | 元知识+公式+变量 |
| 高级查询 | `/cypher_subgraph/`, `/risk_path/`, `/relation_distribution/`, `/related_company_network/`, `/graph_stats/`, `/risk_case_data/` | 图谱分析与风险案例 |
| 关系抽取 | `/extract_relation/`, `/query_relations/`, `/get_companies/`, `/add_company/` | 新闻关系抽取 |

### 2.4 视图层（views.py）

[views.py](knowledgegraph/knowledgegraph/views.py) 是后端核心，约 1590 行，包含全部业务逻辑。关键模块分析如下：

#### 2.4.1 Neo4j 连接

```python
graph = Graph("neo4j://10.176.22.62:7687", auth=("neo4j", "neo4j6008"))
matcher = NodeMatcher(graph)
```

**问题**：连接配置硬编码，且 `graph` 与 `matcher` 在文件顶部被**重复初始化两次**（第 41-42 行与第 44-45 行）。

#### 2.4.2 节点查询逻辑

`Querynodes()` 函数支持多字段模糊匹配（公司名、信用代码、英文名、法人、证券代码、股票简称），使用 `coalesce(..., '') CONTAINS '...'` 处理空值。

**严重问题**：查询条件通过 **f-string 拼接**直接写入 Cypher 语句，存在 **Cypher 注入风险**：
```python
query_conditions.append(f"coalesce(n.公司中文名称, '') CONTAINS '{value}'")
query = f"MATCH (n:Company) WHERE {query_condition_str} RETURN n"
```
相比之下，`fuzzymatch()` 和 `fmatexcel()` 正确使用了参数化查询（`$keyword`），说明开发者了解正确做法但未统一应用。

#### 2.4.3 关系查询逻辑

`QueryRelationship()` 使用社会信用代码定位节点，`QueryRelationship_byname()` 使用公司名定位。前者在结果为空时会因 `result[0]['r']` 抛 `IndexError`，缺少空结果保护。

#### 2.4.4 元知识管理（MetaKnowledgeViewSet）

实现了完整的 CRUD：
- `create`：事务化创建 MetaKnowledge + Formula + Variable，解析 `"1_x_公司负债率"` 格式的变量字符串
- `destroy`：级联删除公式与变量关系，维护引用计数
- `retrieve`：返回元知识详情含公式与变量

#### 2.4.5 风险案例数据（RiskCaseDataView）

基于**巴塞尔协议 III** 框架，将图谱数据分类为五大风险类型 × 三个子类：

| 风险类型 | 数据来源 | 子类数 |
|---------|---------|--------|
| 市场风险 | MetaKnowledge 节点（core_conclusion 关键词匹配） | 3 |
| 信用风险 | GUARANTEES / PLEDGE 关系 | 3 |
| 操作风险 | 违规事件关系 + MetaKnowledge | 3 |
| 流动性风险 | MetaKnowledge 关键词匹配 | 3 |
| 声誉风险 | 违规事件 + Litigation 节点 + ST 公司 | 3 |

每个子类返回最多 5 条案例及总数统计。该视图每次请求实时执行约 15 条 Cypher 查询，无缓存。

#### 2.4.6 图谱统计（GraphStatsView）

提供节点/关系总数、标签分布、关系类型分布、上市状态、行业分布（19 类关键词归类）等指标。行业分类使用关键词匹配将长文本归入标准行业。

### 2.5 关系抽取器（relation_extractor.py）

[relation_extractor.py](knowledgegraph/knowledgegraph/relation_extractor.py) 实现基于 LLM 的新闻关系抽取：

- **公司识别**：基于已知公司列表做字符串包含匹配（非 NER）
- **关系分类**：优先调用 Ollama（qwen3.6:27b），失败时降级为关键词匹配
- **关系类型**：定义 20 种关系（起诉、保荐/承销、投资/收购、子公司/控股等）
- **证据提取**：按句号/问号/感叹号分句，提取同时包含两家公司的句子
- **数据存储**：写入 `front/public/cross_doc_dataset_updated.json`

**问题**：
1. `extract_from_news()` 对 n 家公司生成 n×(n-1) 条有向关系，同一对公司会产生双向记录，且无去重
2. 数据集路径耦合到前端目录（`front/public/`），后端依赖前端目录结构
3. 公司识别依赖完整字符串匹配，无法识别简称/别名

### 2.6 公司名匹配模块（companynameparser）

[namematcher.py](knowledgegraph/knowledgegraph/companynameparser/namematcher.py) 实现公司名相似度匹配，包含繁简转换、银行名归一、括号处理、关键词去除等功能，基于 BERT 向量余弦相似度。

**严重问题**：
- `BERT_PATH = r'D:\huggingface_model\bert-base-chinese'` 硬编码 **Windows 路径**，在 Linux 环境必然报错
- BERT 模型在模块导入时即加载（`model = BertModel.from_pretrained(BERT_PATH)`），重量级依赖
- 该模块在 `views.py` 中被注释掉（第 10 行），实际未启用

### 2.7 权限体系

- **全局**：`DEFAULT_PERMISSION_CLASSES = [AllowAny]`
- **视图级**：绝大多数视图 `permission_classes = []`（无权限限制）
- **少数保护**：仅 `MetaKnowledgeViewSet` 等通过 `IsNeo4jUser`/`IsMetaKnowledgeUser` 校验用户类型
- **装饰器**：`neo4j_user_required`/`metaknowledge_user_required` 已定义但未在任何视图使用

**结论**：权限体系基本形同虚设，节点/关系/抽取接口均无需登录即可访问。

---

## 三、前端架构分析

### 3.1 入口与全局配置（main.js）

[main.js](front/src/main.js) 完成以下全局配置：
- Element Plus 注册
- ResizeObserver 循环警告抑制（猴补丁 + 错误拦截）
- axios 全局配置：`baseURL`（开发环境为空，生产为 `http://10.176.22.62:8000`）、`withCredentials = true`
- 从 `localStorage.jwt_token` 读取 token 设置 Authorization 头

### 3.2 路由与守卫（router/index.js）

[router/index.js](front/src/router/index.js) 定义 13 条路由，仅 `/home` 标记 `requireAuth: true`。

**严重问题——Token 键名不一致**：
- [main.js:52](front/src/main.js#L52) 读取 `jwt_token` 设置请求头
- [Login_neo4j.vue:72](front/src/views/Login_neo4j.vue#L72) 登录成功写入 `access_token`
- [router/index.js:95](front/src/router/index.js#L95) 守卫读取 `access_token`

三者键名不统一，导致**刷新页面后 axios 请求头不会携带 token**（main.js 读 `jwt_token` 为空），但路由守卫仍能通过（读 `access_token` 有值），造成"已登录但请求未认证"的矛盾状态。

### 3.3 主容器（HomeView.vue）

[HomeView.vue](front/src/views/HomeView.vue) 采用 Element Plus 布局，左侧菜单切换主区域组件（`<component :is="currentComponent">`）。通过 `extends` KnowledgeGraphTest 创建了 4 个带不同 `initialTab` 的包装组件，实现菜单直达不同 Tab。

### 3.4 API 调用地址混乱

这是前端最突出的问题。经全量扫描，前端存在**三套并行的 API 地址方案**：

| 地址模式 | 使用页面 | 数量 |
|---------|---------|------|
| `http://10.176.22.62:8001/...` | Login、QueryNode、DeleteNode、AddNode、AllMetaknowledge、AddMetaKnowledge、Fuzzy_match、SubGraph、QueryRelationship、QueryNode_excel、CompareName、CrossDocEntityExtract | ~20 处 |
| `http://localhost:8001/...` | AddRelationship_excel、AddNode_excel | ~4 处 |
| `/api/...`（走 vue.config.js 代理） | KnowledgeGraphTest | 2 处 |
| 直连 Neo4j `http://localhost:7474` | AllGraph | 1 处 |

仅 `KnowledgeGraphTest.vue` 统一使用了 `/api/` 代理前缀，其余页面全部硬编码绝对地址。迁移环境时需逐文件修改。

### 3.5 核心功能页面（KnowledgeGraphTest.vue）

该文件约 2400 行，是功能最丰富的页面，包含 5 个 Tab：
1. **实体分布统计**：总数卡片、饼图（D3.js）、行业分布、违规分析
2. **图谱金融风险知识**：风险总结（五大风险类型卡片，点击展开案例详情）
3. **图谱浏览与性能**：子图查询、N 跳遍历
4. **数据导入与抽取**：节点/关系导入、新闻抽取
5. **本体导出**：JSON Schema / RDF / Turtle 格式导出

该页面 API 调用统一使用 `/api/` 前缀，是前端规范化程度最高的页面。

### 3.6 跨文档抽取页面（CrossDocEntityExtract.vue）

- 从 `/cross_doc_dataset.json` 加载抽取结果
- 调用 `/extract_relation/` 提交新闻进行关系抽取
- 调用 `/add_triples_to_graph/` 将三元组写入图谱（**该后端接口不存在**）

---

## 四、离线脚本分析

### 4.1 图谱扫描脚本（scan_*.py）

根目录下 9 个扫描脚本，功能覆盖：

| 脚本 | 功能 |
|------|------|
| scan_kg_data.py | 基础统计（节点/关系总数） |
| scan_kg_detailed.py | 违规事件行业分布、诉讼案件 |
| scan_kg_detailed2.py | 违规类型细化、诉讼金额区间 |
| scan_universal.py | 全量遍历所有 label 和关系 type |
| scan_deep_fields.py | MetaKnowledge/PLEDGE/GUARANTEES 详细属性 |
| scan_financial_risk.py | 9 类风险分类 |
| scan_extra_fields.py | 实际控制人、资本背景、省份 |
| scan_more_finer.py | 处罚金额区间、处分措施 |
| scan_p25x_full.py | P25xx 违规编码全量扫描 |
| scan_risk_cases_for_ui.py | 生成前端风险案例数据 |

**共性问题**：
- Neo4j 连接全部硬编码 `neo4j://10.176.22.62:7687`
- 大量裸 `except:` 捕获（吞掉所有异常）
- `scan_risk_cases_for_ui.py` 与 `views.py` 的 `RiskCaseDataView` 存在大量重复代码

### 4.2 离线关系预测（laguanxi.py）

[lsydata/data/financedata/laguanxi.py](lsydata/data/financedata/laguanxi.py) 是独立离线脚本：
- 读取 `完整_已知实体匹配/` 下输入
- 调用 Ollama（qwen2.5:32b）对每对公司预测 7 类关系
- 输出到 `完整_已知实体匹配_关系预测结果1/`
- 支持**断点续跑**（通过 `头尾实体.csv` 记录已处理对）

**问题**：输入/输出路径硬编码为 `/home/lushiyin/data/financedata/...`。

---

## 五、问题汇总与风险评级

### 5.1 安全问题（高危）

| 编号 | 问题 | 位置 | 风险 |
|------|------|------|------|
| S1 | `SECRET_KEY` 硬编码 | settings.py:18 | 高 |
| S2 | `DEBUG = True` + `ALLOWED_HOSTS = ['*']` | settings.py:71-73 | 高 |
| S3 | Neo4j 凭据硬编码（neo4j/neo4j6008） | views.py:41 | 高 |
| S4 | 全局 `AllowAny` + 多数视图无权限 | settings.py:100, views.py 各视图 | 高 |
| S5 | Cypher 注入（f-string 拼接用户输入） | Querynodes, QueryRelationship, QueryRelationship_byname 等 | 高 |
| S6 | 默认用户密码硬编码 | create_custom_users.py:8-10 | 中 |
| S7 | 自定义 CORS 中间件设置 `Access-Control-Allow-Origin: '*'` | middleware.py:8 | 中 |

### 5.2 代码质量问题（中危）

| 编号 | 问题 | 位置 |
|------|------|------|
| Q1 | 前端 API 地址三套并存，未统一 | 全部 views |
| Q2 | Token 键名不一致（jwt_token / access_token） | main.js, Login, router |
| Q3 | serializers.py `VariableSerializer` 字段名错误（`variable_type` 应为 `variable_meaning`） | serializers.py:9 |
| Q4 | `graph`/`matcher` 重复初始化 | views.py:41-45 |
| Q5 | namematcher.py 硬编码 Windows BERT 路径 | namematcher.py:34 |
| Q6 | BERT 模型导入时加载（重量级） | namematcher.py:35-37 |
| Q7 | 自定义 CorsMiddleware 未注册到 MIDDLEWARE（死代码） | middleware.py |
| Q8 | create_metaknowledge.py 引用已删除字段 `formula` | create_metaknowledge.py:25 |
| Q9 | uwsgi.ini 硬编码旧路径 `/home/wangsiyuan/...` | uwsgi.ini:4 |
| Q10 | `QueryRelationship()` 空结果未保护，抛 IndexError | views.py:180 |
| Q11 | `extract_from_news()` 生成 O(n²) 双向关系，无去重 | relation_extractor.py:233-247 |
| Q12 | 扫描脚本大量裸 `except:` | scan_*.py |
| Q13 | CrossDocEntityExtract 调用不存在的 `/add_triples_to_graph/` 接口 | CrossDocEntityExtract.vue:538 |

### 5.3 架构问题（中危）

| 编号 | 问题 | 说明 |
|------|------|------|
| A1 | 无环境变量配置 | 所有配置硬编码，无 `.env` 机制 |
| A2 | 无容器化支持 | 无 Dockerfile / docker-compose |
| A3 | 后端依赖前端目录 | relation_extractor 数据集路径指向 `front/public/` |
| A4 | Celery 已配置但未使用 | 视图中无异步任务调用，批量导入仍同步阻塞 |
| A5 | Celery broker 端口非标准 | `redis://localhost:8000/0`（常见 Redis 为 6379） |
| A6 | 图谱节点与 Django 模型分离 | Company/Violation/Litigation 在 Neo4j，元知识在 SQLite，无统一抽象 |
| A7 | RiskCaseDataView 每次请求实时查 15 条 Cypher | 无缓存，高并发风险 |

### 5.4 代码重复

| 重复内容 | 位置 |
|---------|------|
| 风险案例数据生成逻辑 | `scan_risk_cases_for_ui.py` 与 `views.py:RiskCaseDataView` 几乎完全相同 |
| Neo4j 连接初始化 | views.py（2 次）、9 个 scan 脚本（各 1 次） |
| 模糊匹配查询 | `fuzzymatch()` 与 `fmatexcel()` 查询语句相同 |

---

## 六、改进建议

### 6.1 安全加固（优先级：最高）

1. **引入环境变量**：使用 `python-dotenv`，将 `SECRET_KEY`、Neo4j 凭据、CORS 白名单、Celery 配置等全部移入 `.env`
2. **关闭生产调试**：`DEBUG = False`，收紧 `ALLOWED_HOSTS`
3. **修复 Cypher 注入**：所有动态查询改用参数化（`$param`），参考 `fuzzymatch()` 的实现
4. **启用权限控制**：将 `DEFAULT_PERMISSION_CLASSES` 改为 `IsAuthenticated`，按视图配置具体权限
5. **轮换凭据**：更换 Neo4j 密码与默认用户密码

### 6.2 前端规范化（优先级：高）

1. **统一 API 调用**：所有页面使用 `/api/` 代理前缀，删除硬编码绝对地址
2. **修复 Token 键名**：统一为 `access_token`，确保 main.js、Login、router 三处一致
3. **封装 axios 实例**：统一拦截器处理 token 注入与错误响应

### 6.3 代码修复（优先级：中）

1. 修复 `serializers.py` 的 `variable_type` -> `variable_meaning`
2. 删除 `views.py` 重复的 `graph`/`matcher` 初始化
3. 删除未注册的 `middleware.py` 自定义中间件
4. 修复 `namematcher.py` 的 BERT 路径（改环境变量或移除该模块）
5. 为 `QueryRelationship()` 增加空结果保护
6. 修复 `create_metaknowledge.py` 或将其删除

### 6.4 架构优化（优先级：中）

1. **解耦后端与前端目录**：关系抽取数据集改存后端独立目录或数据库
2. **引入缓存**：`RiskCaseDataView` 和 `GraphStatsView` 的统计结果加 Redis 缓存（已有 Celery/Redis 依赖）
3. **启用 Celery**：批量导入（`AddNodeExcelView`/`AddRelationshipExcelView`）改为异步任务
4. **容器化**：提供 Dockerfile 与 docker-compose，固化运行环境
5. **消除重复**：将 `scan_risk_cases_for_ui.py` 的逻辑抽取为共享模块

### 6.5 关系抽取优化（优先级：低）

1. 公司识别改用 NER 模型替代字符串匹配
2. `extract_from_news()` 对公司对去重（`combinations` 替代双层循环）
3. 关系类型与离线脚本（laguanxi.py 的 7 类）统一

---

## 七、总体评价

### 7.1 优点

- **功能完整**：覆盖图谱 CRUD、元知识管理、关系抽取、统计分析、风险展示全链路
- **风险分析框架清晰**：基于巴塞尔协议 III 的五类风险分类具有金融领域专业性
- **LLM 集成**：利用本地 Ollama 进行关系抽取，兼顾隐私与成本
- **前端可视化丰富**：KnowledgeGraphTest 页面集成统计图表、风险卡片、本体导出等多种可视化
- **断点续跑**：离线脚本 laguanxi.py 支持断点续跑，适合大规模数据处理

### 7.2 主要不足

- **工程化程度低**：无环境变量、无容器化、配置全硬编码，迁移成本高
- **安全防护薄弱**：权限几乎完全放开，存在注入风险，密钥/凭据明文
- **前后端契约松散**：API 地址硬编码分散，Token 机制不一致，存在调用不存在接口的情况
- **代码规范不统一**：重复代码多、死代码存在、部分模块字段名错误
- **测试缺失**：仅有 test_relation_extractor.py 和 test_ollama_relation.py 两个测试文件，无系统性测试

### 7.3 可维护性评级

| 维度 | 评级 | 说明 |
|------|------|------|
| 功能完整性 | ★★★★☆ | 功能覆盖全面 |
| 代码安全性 | ★☆☆☆☆ | 多处高危安全风险 |
| 代码规范性 | ★★☆☆☆ | 硬编码多、重复多、不一致 |
| 可部署性 | ★★☆☆☆ | 无容器化、配置硬编码 |
| 可维护性 | ★★☆☆☆ | 缺测试、缺文档统一性 |
| 技术先进性 | ★★★★☆ | LLM 集成、D3 可视化 |

---

## 八、附录：关键文件索引

| 文件 | 行数 | 说明 |
|------|------|------|
| [knowledgegraph/views.py](knowledgegraph/knowledgegraph/views.py) | ~1590 | 后端全部业务逻辑 |
| [front/src/views/KnowledgeGraphTest.vue](front/src/views/KnowledgeGraphTest.vue) | ~2400 | 前端核心功能页 |
| [knowledgegraph/relation_extractor.py](knowledgegraph/knowledgegraph/relation_extractor.py) | 300 | LLM 关系抽取 |
| [knowledgegraph/settings.py](knowledgegraph/knowledgegraph/settings.py) | 175 | 后端配置 |
| [knowledgegraph/urls.py](knowledgegraph/knowledgegraph/urls.py) | 71 | 路由定义 |
| [knowledgegraph/models.py](knowledgegraph/knowledgegraph/models.py) | 81 | 数据模型 |
| [front/src/main.js](front/src/main.js) | 68 | 前端入口 |
| [front/src/router/index.js](front/src/router/index.js) | 114 | 前端路由 |
| [lsydata/data/financedata/laguanxi.py](lsydata/data/financedata/laguanxi.py) | - | 离线关系预测 |

---

*报告完*
